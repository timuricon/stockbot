Roadmap — NYSE/NASDAQ Trading Bot

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ ЗАВЕРШЕНО
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Stage 1 — Architecture Stabilization
✅ Формализована архитектура
✅ Определены модули и зоны ответственности
✅ Устранены циклические импорты

Stage 2 — Modular Refactoring
✅ Вынесен data_provider (polygon_client, yfinance_client, bars, cache)
✅ Вынесены scanners (unified, base, superstock, growth, early_trend)
✅ Вынесен portfolio (io, analysis, manager, universe)
✅ Вынесен analytics (signal_evaluator, superstock_history)
✅ Разбит telegram_ui/handlers.py на модули:
   - handlers/base.py — start, stop, error_handler
   - handlers/scans.py — кнопки сканов
   - handlers/portfolio.py — pb_* кнопки
   - handlers/conversations.py — ConversationHandler
   - scan_threads.py — фоновые потоки сканов

Stage 3 — Signal Quality (Фаза 1)
✅ Динамический апсайд (ATR-based) в Swing модуле
✅ EMA скоринг (4-уровневая система: зоны A/B/C/D)
✅ Top-10 near-misses для диагностики фильтров
✅ ATR-фильтр пробоя в паттернах (Cup&Handle, Range Breakout)
✅ Volume Contraction в Early Trend
✅ Детальная диагностика swing (_swing_diag) в логах

Stage 4 — Infrastructure
✅ Дисковый кэш баров (bars_cache/) с инкрементальным обновлением
✅ Universe тикеров (tickers_universe.json, TTL 7 дней)
✅ Pre-filter кэш (prefilter_cache.json, TTL 4 часа)
✅ Логирование в bot.log (FileHandler + защита от дублирования)
✅ Фикс HTTP 400 (start > end при запросе баров)
✅ Установка yfinance — consensus_tp теперь работает
✅ APScheduler: автозапуск unified скана Пн–Пт 13:00 CET

Stage 5 — Analytics
✅ Signal Evaluator: P&L оценка на +5/+10/+20 дней по снапшотам
✅ Superstock History: персистентность кандидатов + обновление цен
   (сент. 2026: save_snapshot подключён к скану — ранее запись не вызывалась)
✅ Статусы кандидатов: active / expired / hot / target
✅ Сохранение снапшотов unified скана → analytics_history/

Stage 6 — Сентябрь 2026: стабилизация
✅ ET-only режим (кнопка, top-10 Early Trend, порог 8)
✅ Динамический порог: below_ema > 40% → порог 60 → 52
✅ Оценка сигналов: честный отчёт, фоновый запуск, автозапуск Пн–Пт 20:00 CET,
   pacing 13с + retries=2 под free tier, накопленная статистика
✅ Портфель: yfinance-вызовы вне event loop, таймаут ребаланса, атомарная
   запись portfolio_b.json + .bak, pb_edit сохраняет одобрения
✅ Логирование: INFO + ротация 5МБ×3; секреты убраны из кода (ротация ключей —
   на пользователе), git-репозиторий, requirements.txt, README
✅ Базовые unit-тесты (signal_evaluator, portfolio_io, smoke-импорты)

Stage 7 — Сентябрь 2026: аудит-фиксы (report4)
✅ Growth-сканер удалён как мёртвый код: недостижим из меню, дублировал
   Momentum-модуль unified. Momentum-логика (patterns.py, GROWTH_*-константы,
   get_growth_weekly_bars_df) сохранена — используется unified сканером.
✅ cancel_event протянут до конца: run_unified_scan / run_et_only_scan /
   run_superstock_scan принимают cancel_event, проверяют его между тикерами
   (до Polygon-вызовов), паузы прерываемые. /stop и кнопка «❌ Отменить скан»
   реально останавливают сканы; частичные результаты доставляются с пометкой.
✅ Unit-тесты: cancel_event (прерывание после 1-й итерации), чистые функции
   filters / patterns / early_trend

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 В РАБОТЕ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Диагностика качества сигналов:
- Оценено 97 сигналов (46 снапшотов, май–август 2026) с бенчмарком QQQ
  и разбивкой по зонам входа. Итог первого среза:
  * Весь пул: α −0.3/−0.2/−0.8% (d5/d10/d20), рынок обогнали 41–48% —
    суммарно сигнал не обыгрывает QQQ.
  * Зона A (у EMA, n=49): α +0.6/+1.2/−0.3% — РАБОТАЕТ на d5/d10,
    edge затухает к d20.
  * Зона B (8–20% над EMA, n=45): α −1.1/−1.4/−0.5% — главный источник
    просадки пула, вход «по продолжению» уже поздний.
  * Зона C (>20%, n=3): α до −13% — подтверждает направление, но выборка мала.
  Вывод: edge сканера сконцентрирован в зоне A; тактика — вход у поддержки,
  горизонт 5–10 дней. Практика до правок кода: брать только 🟢 Зона A.
- Симуляция лимит-выхода (сент. 2026): весь пул — лимитки не бьют QQQ
  (лучший уровень +5% даёт +0.73% vs QQQ +0.8%). НО зона A + лимитка +5%:
  дошли 59.2%, средний итог +2.35%/сделку (недошедшие в среднем −1.5%) —
  втрое выше QQQ. Рабочий протокол: только зона A → лимитка +5% →
  тайм-стоп 20 торговых дней.
- Накопление продолжается (автооценка Пн–Пт 20:00 CET). Через ~месяц —
  проверка, держится ли «зона A + лимитка +5%» ≈ +2% на свежих данных.
- ⚠️ Предрегистрация критериев (записано ДО появления свежих данных,
  чтобы не двигать планку постфактум). Протокол «зона A + лимитка +5%»
  считается ПОДТВЕРЖДЁННЫМ, если к 15.10.2026 на сигналах с датой
  ≥ 12.09.2026 выполняется ВСЁ:
  * свежих zone A сигналов n ≥ 30;
  * средний итог лимитки +5% на них ≥ +1.0%;
  * доля дошедших ≥ 45%;
  * α зоны A на d10 ≥ +0.5%.
  Если подтверждено → строим watchlist-алерты (откат в зону A) и
  рассматриваем резку балла зоны B. Если нет → сканер остаётся
  генератором идей для ручного анализа, вопрос закрыт до пересмотра
  гипотез. Помнить: max_move считается по дневным закрытиям и не видит
  внутридневные касания уровня — смещение симуляции по направлению
  неизвестно.
- Старый вывод «ET-only не добирают до порога 60» — заменён этим анализом.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 ЗАПЛАНИРОВАНО (приоритет)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Watchlist / Алерты (высокий приоритет, обоснован данными по зонам)
   Алерт, когда тикер из недавнего скана откатывается в зону A
   (0–8% над 30W EMA) — вход у поддержки. Протокол сделки:
   лимитка +5%, тайм-стоп 20 торговых дней (см. симуляцию выше).
   Дополнительно: уведомление при пробое EMA200.
   ⚠️ Правки скоринга сканера (резка балла зоны B) — только после
   подтверждения результата на накопленных данных (месяц свежих оценок).

2. Расширение unit-тестов (средний приоритет)
   data_provider: тесты bars, cache, polygon_client
   scanners: тесты фильтров, паттернов
   Цель: покрытие ≥ 60%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔮 ДОЛГОСРОЧНО
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Signal Quality (Фазы 2-15):
- Penalty-система на основе evaluated снапшотов
- Весовые коэффициенты в Unified Score (backtesting)
- Недельная структура (weekly HH/HL) в Swing
- Импульсная структура (impulse > pullback) в Momentum
- Качество пробоя EMA200 в Early Trend
- Volume trend (качество объёма) в Momentum
- Pre-filter ликвидности

Risk Management:
- Risk-manager модуль
- Position-sizing
- Stop-logic и exit-logic

Analytics:
- Backtesting engine на исторических данных
- Оптимизация параметров на основе backtesting
- ML-скоринг сигналов

Infrastructure:
- Параллельные запросы (asyncio) при переходе на платный Polygon
- CI/CD pipeline
- Monitoring и alerting

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Technical Debt
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Unit-тесты: покрытие неполное (цель ≥ 60%); базовые + filters/patterns/
  early_trend/cancel_event уже есть
- Нет backtesting engine
- Нет real-trading engine
- Нет полноценного risk-manager
- Нет CI/CD pipeline
- Бесплатный Polygon (5 req/min): скан 30–90+ минут вместо ~5 минут;
  рассмотреть Basic-тариф, если сканер развивается всерьёз

Закрыто (сент. 2026):
- [x] cancel_event протянут через unified/superstock/et_only сканы —
  отмена кнопкой «❌ Отменить скан» и через /stop работает
